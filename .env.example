# Server Angel Configuration Template
# Copy this file to .env and replace all values with your actual configuration

# ============================================================================
# PROJECT PATHS (REQUIRED)
# ============================================================================
# Absolute path to your Django/application project
PROJECT_ROOT=/var/www/your-project

# Absolute path to your Python virtual environment
VENV_PATH=/var/www/your-venv

# ============================================================================
# GIT CONFIGURATION
# ============================================================================
# Git remote name (usually 'origin')
GIT_REMOTE=origin

# Production branch to monitor and deploy from
GIT_BRANCH=main

# ============================================================================
# SYSTEMD SERVICE NAMES (REQUIRED)
# ============================================================================
# Nginx service name
NGINX_SERVICE=nginx

# Gunicorn service name (your application server)
GUNICORN_SERVICE=gunicorn

# Redis service name (optional - leave empty if not using)
REDIS_SERVICE=redis

# Celery service name (optional - leave empty if not using)
CELERY_SERVICE=celery

# ============================================================================
# EMAIL CONFIGURATION (REQUIRED)
# ============================================================================
# SMTP server hostname
SMTP_HOST=smtp.gmail.com

# SMTP server port (587 for TLS, 465 for SSL)
SMTP_PORT=587

# SMTP username (usually your email address)
SMTP_USER=your-email@gmail.com

# SMTP password or app-specific password
# For Gmail: Generate app password at https://myaccount.google.com/apppasswords
SMTP_PASSWORD=your-app-password

# Email address to send from
EMAIL_FROM=server-angel@yourdomain.com

# Comma-separated list of recipient email addresses
EMAIL_RECIPIENTS=admin1@example.com,admin2@example.com

# ============================================================================
# HEALTH REPORT SCHEDULE
# ============================================================================
# Morning report time (24-hour format)
MORNING_REPORT=07:00

# Evening report time (24-hour format)
EVENING_REPORT=19:00
